import { uiTokens } from "./tokens.js";

export const nativeTokens = {
  colors: {
    backgroundBase: uiTokens.color.backgroundBase,
    surface: uiTokens.color.backgroundSurface,
    border: "#1e293b",
    textPrimary: uiTokens.color.textPrimary,
    textMuted: uiTokens.color.textMuted,
    textSubtle: uiTokens.color.textSubtle,
    accent: uiTokens.color.accent,
    warning: uiTokens.color.warning,
  },
  spacing: uiTokens.spacing,
  radius: uiTokens.radius,
};
